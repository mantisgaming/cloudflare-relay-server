{
	"name": "totsm-cloudflare-relay",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled -e=development",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"reset-local-db": "wrangler d1 execute RELAY_D1 --local --file=./RELAY_D1_schema.sql"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.62.0"
	},
	"dependencies": {
		"itty-router": "^5.0.22"
	}
}
